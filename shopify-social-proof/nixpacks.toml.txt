[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "npm run docker-start"
```

**Step 2: Also add a `Procfile`** (another file in the same folder):
```
web: npm run docker-start